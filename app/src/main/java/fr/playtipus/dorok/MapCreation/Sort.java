package fr.playtipus.dorok.MapCreation;

import java.util.List;

import fr.playtipus.dorok.Tile;

public class Sort {
    private List<Tile> lSorted;

    public  Sort (List<Tile> lTiles) {
        /*X == 0 && Y le plus petit;
        while ()
        X -= 1;
        Y += 1;
        puis X = 1 && Y le plus petit;
        find puis met a la place i ++ ? */
    }
    public List<Tile> getlTiles() {
        return lSorted;
    }
}